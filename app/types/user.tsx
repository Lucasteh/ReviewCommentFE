export type AuthUser = {
  id: string;
  email: string;
  token: string;
}

export type Product = {
  productName: string;
  productPrice: number;
  productDescription: string;
  productQuatity: number;
}